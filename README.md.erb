# pry-full

Get up and going with a good set of pry tools right away.

<%
  require 'cgi'
  def h unclean; CGI::escapeHTML unclean end
  require 'yaml'
  extras = YAML.load_file 'in.yml'
  plus_in = '../pry-plus/in.yml'
  raise 'Must clone rking/pry-plus into ..' unless File.exists? plus_in
  list = YAML.load_file(plus_in).merge extras
%>

## Dependent Gems

<% list.each do |name, info| %>
### [<%= name %>](<%= info['url'] %>)

<%=h info['desc'] %>

<% if info['use cases'] %>
  <% info['use cases'].each do |e| %>
- <%=h e %>
  <% end %>
<% end %>

<% end %>
